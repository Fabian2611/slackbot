# Slack Bot
A Slack bot in Rust, using a manual WebSocket connection.

## Why?
Because I felt like using a full wrapper around the API was both overkill and cheating - especially with my goal to
learn a bit more about async Rust and networking.
